const config = {
    discord: {
        botToken: "changeme",
        guildId: "changeme"
    },
    modules: {
        discord: {
            enable: true,
            dependencies: [
                
            ]
        },
        logger: {
            enable: true,
            dependencies: [
                
            ]
        }
    }
};

module.exports = {
    config
};